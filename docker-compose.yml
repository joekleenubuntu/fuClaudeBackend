version: '3.8'

services:
  fuclaude-backend:
    build: .
    container_name: fuclaude-backend
    ports:
      - "3000:3000"
    volumes:
      - ./config.json:/app/config.json
      - ./data.json:/app/data.json
    restart: unless-stopped
